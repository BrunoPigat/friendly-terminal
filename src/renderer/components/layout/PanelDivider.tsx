/**
 * Vertical divider between split-view panels.
 * Thick enough to visually separate two distinct project contexts.
 */
export default function PanelDivider() {
  return <div className="w-1.5 shrink-0 bg-neutral-300" />
}
